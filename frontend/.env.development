# Development Environment Configuration
# The backend API URL - can be localhost or your computer's IP address
# Examples:
#   - For localhost only: http://localhost:8000
#   - For network access: http://192.168.1.100:8000 (replace with your computer's IP)
#   - Leave empty or comment out to auto-detect based on current hostname (RECOMMENDED)

# Auto-detect enabled - frontend will connect based on the IP you use to access it
#VITE_API_BASE_URL=http://10.149.122.2:8000
